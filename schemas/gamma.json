{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "darktable/iop/gamma",
  "title": "gamma",
  "description": "Display encoding - converts linear RGB to gamma-corrected output for display",
  "type": "object",
  "version": 1,
  "properties": {
    "gamma": {
      "type": "number",
      "description": "Gamma value (unused - hardcoded sRGB curve)",
      "default": 0.0
    },
    "linear": {
      "type": "number",
      "description": "Linear portion threshold (unused - hardcoded sRGB curve)",
      "default": 0.0
    }
  },
  "additionalProperties": false,
  "note": "This module uses a hardcoded sRGB gamma curve and the params are unused legacy fields"
}
